#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../requiresafe/bin/requiresafe" "$@"
  ret=$?
else 
  node  "$basedir/../requiresafe/bin/requiresafe" "$@"
  ret=$?
fi
exit $ret
